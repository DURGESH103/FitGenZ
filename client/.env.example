# ── API base (Vite proxies /api → backend in dev, so this is only needed for production)
VITE_API_URL=http://localhost:5000

# ── Web Push VAPID public key (must match server VAPID_PUBLIC_KEY)
VITE_VAPID_PUBLIC_KEY=
